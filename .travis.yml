language: python
os: linux

python:
    - "3.6"

install:
    - pip install -r requirements.txt

script:
    DIR=examples
    FILES=${DIR}/*.mid
    for f in $FILES do
        python -O process_music/process_music.py ${DIR}/${f}
    loop