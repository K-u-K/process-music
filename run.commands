docker build -t github.com/k-u-k/process-music .
docker run --rm --name process-music -v $(pwd):/app github.com/k-u-k/process-music
docker run --rm --name process-music -v $(pwd):/app github.com/k-u-k/process-music -O process_music/process_music.py some_midi_file.mid